
#include <stdio.h>
int main()
{
    int x = 9, y = 6;
    printf("Before Swapping x=%d, y=%d\n",x,y); 
    x = x + y; //x=9+6-->15
    y = x - y; // y=15-6-->9
    x = x - y; // x=15-9-->6
 
    printf("After Swapping: x = %d, y = %d", x, y);
 
    return 0;
}
